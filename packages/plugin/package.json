{
  "name": "@metatoy/figtree-plugin",
  "version": "1.1.0",
  "private": true,
  "description": "Figma plugin that previews proposed design tokens in your app via the Figtree bridge",
  "scripts": {
    "build": "echo \"no build step — plain code.js + ui.html loaded directly by Figma\" && exit 0"
  }
}
